# Generated by Django 6.0.7 on 2026-08-16 09:47

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0059_slots_and_picks_say_slot_type_and_pickable"),
    ]

    operations = [
        migrations.AlterField(
            model_name="defaultassignment",
            name="default_pickable",
            field=models.ForeignKey(
                blank=True,
                help_text="A slot's starting pick — what the choice arrives already settled on.",
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="+",
                to="library.pickable",
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="assigned_to",
            field=models.CharField(
                choices=[("bearer", "the bearer"), ("gang", "the gang")],
                default="bearer",
                help_text="Where the pick lands. Almost always the bearer; assigned to the gang, the pick is the gang's and is broadcast (but not displayed) to every member, whoever was asked.",
                max_length=20,
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="hidden",
            field=models.BooleanField(
                default=False,
                help_text="Display no choice at all. What is picked still applies.",
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="label",
            field=models.CharField(
                blank=True,
                default="",
                help_text='The name on this slot, e.g. "Gang Legacy". Blank uses this slot\'s own name.',
                max_length=200,
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="min_picks",
            field=models.PositiveIntegerField(
                default=1, help_text="How many picks expected. Nought asks for nothing."
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="picklist",
            field=models.ForeignKey(
                help_text="The picklist this slot draws from.",
                on_delete=django.db.models.deletion.PROTECT,
                related_name="slots",
                to="library.picklist",
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="slot_type",
            field=models.ForeignKey(
                help_text="The type of slot being configured.",
                on_delete=django.db.models.deletion.PROTECT,
                related_name="slots",
                to="library.slottype",
            ),
        ),
        migrations.AlterField(
            model_name="slottype",
            name="allows_repeats",
            field=models.BooleanField(
                default=True,
                help_text="Whether one holder may pick the same pickable for two slots of this type.",
            ),
        ),
    ]
