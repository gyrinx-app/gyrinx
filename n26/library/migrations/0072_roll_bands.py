# Generated by Django 6.0.7 on 2026-08-29 08:20

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0071_delete_lasting_effect"),
    ]

    operations = [
        migrations.AddField(
            model_name="picklist",
            name="dice",
            field=models.CharField(
                blank=True,
                choices=[("d3", "D3"), ("d6", "D6"), ("d66", "D66"), ("2d6", "2D6")],
                default="",
                help_text="The die this table is rolled on, where it is one. Blank is an ordinary list, chosen from rather than rolled.",
                max_length=8,
            ),
        ),
        migrations.AddField(
            model_name="picklist",
            name="roll_selects",
            field=models.CharField(
                blank=True,
                choices=[
                    ("band", "The one row the roll lands in"),
                    ("threshold", "Every row at or below the roll"),
                ],
                default="",
                help_text="How a roll finds its result on this table.",
                max_length=16,
            ),
        ),
        migrations.AddField(
            model_name="picklistmember",
            name="roll_high",
            field=models.PositiveSmallIntegerField(
                blank=True,
                help_text="The highest roll that lands here. The same as the lowest for one roll.",
                null=True,
            ),
        ),
        migrations.AddField(
            model_name="picklistmember",
            name="roll_low",
            field=models.PositiveSmallIntegerField(
                blank=True,
                help_text="The lowest roll that lands here, on a roll table.",
                null=True,
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="label",
            field=models.CharField(
                blank=True,
                default="",
                help_text='What the card calls this choice — the heading on its row, and what the Choose control asks for, e.g. "Lasting Injuries". Blank uses this slot\'s own name.',
                max_length=200,
            ),
        ),
        migrations.AddConstraint(
            model_name="picklistmember",
            constraint=models.CheckConstraint(
                condition=models.Q(
                    models.Q(("roll_high__isnull", True), ("roll_low__isnull", True)),
                    models.Q(
                        ("roll_high__isnull", False),
                        ("roll_low__isnull", False),
                        ("roll_low__lte", models.F("roll_high")),
                    ),
                    _connector="OR",
                ),
                name="picklist_member_band_is_whole",
            ),
        ),
    ]
