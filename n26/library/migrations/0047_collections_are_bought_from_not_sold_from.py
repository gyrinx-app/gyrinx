# Generated by Django 6.0.7 on 2026-08-13 08:33

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0046_a_rule_may_refile_a_model"),
    ]

    operations = [
        migrations.AlterField(
            model_name="affiliation",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="archetype",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="collection",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="collection",
            name="prices_its_entries",
            field=models.BooleanField(
                default=True,
                help_text="On for anything bought from — prices and Trade Points may be stated per entry. Off for a menu, like a pick list behind a choice: nothing is for sale, so listing an item asks for nothing but the item.",
                verbose_name="Prices its entries",
            ),
        ),
        migrations.AlterField(
            model_name="counter",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="gangtype",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="hidden",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="lastingeffect",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="power",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="profile",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="rule",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="skill",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="skilltree",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="specialisation",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="subtype",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="trait",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="wargear",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="weapon",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="weaponaccessory",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
        migrations.AlterField(
            model_name="weaponprofile",
            name="is_exclusive",
            field=models.BooleanField(
                default=False,
                help_text='TP "E": never offered at the Trading Post — equipment list only. Wins over any trade point price.',
            ),
        ),
    ]
