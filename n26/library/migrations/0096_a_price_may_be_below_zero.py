# Generated by Django 6.0.7 on 2026-09-07 20:18

from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0095_a_category_may_draw_its_own_row_on_a_card"),
    ]

    operations = [
        migrations.AlterField(
            model_name="affiliation",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="asset",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="campaigntype",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="collection",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="collectionentry",
            name="price_override",
            field=models.IntegerField(
                blank=True,
                help_text="This list's credit price. Blank means at reference price.",
                null=True,
            ),
        ),
        migrations.AlterField(
            model_name="counter",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="gangtype",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="hidden",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="pickable",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="power",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="profile",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="rule",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="skill",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="slot",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="subtype",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="trait",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="wargear",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="weapon",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="weaponaccessory",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AlterField(
            model_name="weaponprofile",
            name="price",
            field=models.IntegerField(
                default=0,
                help_text="Credit price at reference — what the catalogue prints. Gear may be priced below zero, for something that makes its holder worth less. A fighter may not.",
            ),
        ),
        migrations.AddConstraint(
            model_name="collectionentry",
            constraint=models.CheckConstraint(
                condition=models.Q(
                    ("profile__isnull", True),
                    ("price_override__isnull", True),
                    ("price_override__gte", 0),
                    _connector="OR",
                ),
                name="collection_entry_profile_price_is_not_below_zero",
            ),
        ),
        migrations.AddConstraint(
            model_name="profile",
            constraint=models.CheckConstraint(
                condition=models.Q(("price__gte", 0)),
                name="profile_price_is_not_below_zero",
            ),
        ),
    ]
