# Generated by Django 6.0.7 on 2026-08-15 11:31

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("library", "0055_an_offer_says_where_the_pick_will_be_assigned"),
    ]

    operations = [
        migrations.AlterField(
            model_name="collectionsection",
            name="name",
            field=models.CharField(
                help_text='This collection\'s own heading — "Primary".', max_length=200
            ),
        ),
        migrations.AlterField(
            model_name="defaultassignment",
            name="amount",
            field=models.PositiveIntegerField(
                default=0,
                help_text='A counter\'s opening value — the 61 in "Starting XP 61".',
            ),
        ),
        migrations.AlterField(
            model_name="offerschoice",
            name="from_section",
            field=models.ForeignKey(
                blank=True,
                help_text='Narrow the offer to one section of a collection: "a Skill from a set that is Primary for this fighter". That section is part of the collection\'s own schema, the same row a placement aims at, so the admin picks "Primary (Skills & Powers)" rather than restating a name. Blank offers the whole kind.',
                null=True,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="+",
                to="library.collectionsection",
            ),
        ),
        migrations.AlterField(
            model_name="placescategory",
            name="section",
            field=models.ForeignKey(
                help_text='The collection section it appears under for the bearer — one of the collection\'s own sections, so the admin picks "Primary (Skills & Powers)" rather than restating a string and a number. Scopes the placement to that collection.',
                on_delete=django.db.models.deletion.PROTECT,
                related_name="+",
                to="library.collectionsection",
            ),
        ),
    ]
